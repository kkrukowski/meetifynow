{
  "name": "time2meet",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.0",
    "prettier": "^3.0.3"
  },
  "scripts": {
    "dev": "concurrently \"cd server && yarn run start\" \"cd client && yarn run dev\"",
    "dev-client": "cd client && yarn run start",
    "dev-server": "cd server && yarn run start"
  }
}
